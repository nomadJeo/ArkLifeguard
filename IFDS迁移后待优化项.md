# IFDS 迁移后待优化项

## 当前状态

PR-3 已将旧项目使用的 IFDS 基础设施直接迁移到 `src/ifds`，并将 ArkAnalyzer 依赖统一收口到 `src/adapter/arkanalyzer.ts`。当前实现能够通过类型检查、构建、普通 CFG 事实传播测试和既有生命周期回归，但仍以保持旧实现行为为主，尚未进行求解性能、调用解析精度和异常路径方面的优化。

下列事项不属于本次直接迁移的完成条件，后续应分批实现，并在每项改动后单独运行 IFDS、空指针和资源泄漏回归。

## P0：补齐迁移验收与正确性边界

1. **补全 IFDS 独立测试。** 当前只有路径边结构和普通 CFG 传播测试，仍需覆盖单一调用与返回、多调用点共享同一 callee、多目标调用、call-to-return 旁路、回调参数、循环和递归终止、事实语义判等与去重。
2. **统一事实语义判等。** `pathEdgeSet` 已调用 `DataflowProblem.factEqual`，但 `incoming`、`endSummary` 和 `summaryEdge` 的部分查找仍使用事实对象引用相等。后续应统一为问题定义的语义相等，避免内容相同但对象不同的事实丢失 summary 或重复传播。
3. **稳健识别赋值型调用。** 当前调用判断能够从语句表达式中发现调用，但处理阶段仍将调用语句直接视为 `ArkInvokeStmt`。应以 `Stmt.getInvokeExpr()` 为统一入口，并对独立调用和赋值右值调用增加回归。
4. **修正方法入口定位方式。** 当前以参数数量作为基本块语句下标来寻找方法入口，在 `this`、闭包环境、默认参数或 ArkAnalyzer IR 布局变化时不够稳健。应基于 CFG 提供的入口语句和参数引用语句显式定位首条业务语句。
5. **复核调用图上下文。** 当前 CHA 解析使用分析入口方法的调用图节点作为上下文。后续应使用实际调用语句所属方法，并验证虚调用、多目标调用和同名方法不会串联。
6. **明确多返回后继策略。** 当前调用点只取第一个 return site。应确认 ArkIR 中普通返回、异常后继和分支汇合场景的语义，再决定是否传播到全部合法后继。

## P1：求解性能优化

1. **建立路径边哈希索引。** `pathEdgeSetHasEdge` 当前线性扫描全部路径边，事实规模增大后容易形成二次复杂度。可按起点语句、起点事实、终点语句和终点事实建立桶索引，并在桶内继续使用 `factEqual` 防止哈希碰撞。
2. **替换数组头部出队和中部插入。** 当前工作队列使用 `shift()`、线性查找和 `splice()` 区分普通边与调用/返回边。可改为立即队列和延迟队列，在保持原调度顺序的前提下实现常数时间入队、出队。
3. **索引 incoming、end summary 和 summary edge。** 当前多个阶段需要遍历 Map 键或 summary 集合。应按语句与事实语义键建立索引，减少方法调用密集项目中的重复扫描。
4. **缓存端点到路径边的关联。** 返回传播阶段会遍历全部 `pathEdgeSet` 查找与调用点匹配的边，可维护按终点索引的路径边集合。
5. **避免修改 Scene 返回的集合。** `buildStmtMapInClass` 当前直接向 `scene.getMethods()` 的返回数组追加入口方法。应复制后再补入口并去重，避免对 Scene 内部状态产生隐式影响。

## P2：调用与控制流健壮性

1. 支持一个调用点的全部可解析 callee，并区分项目方法、SDK 方法、函数参数回调和无法解析的外部调用。
2. 回调参数解析不应只返回第一个函数参数；需要验证多个回调参数、箭头函数、闭包捕获和函数类型别名。
3. 对无 CFG callee、无 return site、空基本块和不完整签名采用显式旁路策略，避免静默丢失事实。
4. 评估异常控制流、`throw`、Promise/async 边界是否需要进入 IFDS supergraph；未支持前应在分析结果中保留清晰边界。
5. 为递归和大型循环增加规模上限或可观测统计，包括路径边数量、工作队列峰值、求解耗时和截断原因。

## 与空指针迁移的关系

空指针模块迁移时已经保留 `NullnessSolver` 针对路径边去重、工作队列、incoming 和 end summary 的专用索引，没有将其提前合并进通用 `DataflowSolver`。迁移后的 52 个 schema-v2 基准与旧项目结果一致，均为 37 TP、12 TN、0 FP、3 FN；`ExceptionPath`、`HideShow` 和 `EventListener` 仍是既有漏报，不属于迁移回归。后续应先处理这些已知能力缺口，再评估将通用优化下沉到 `src/ifds`，并删除空指针求解器中的重复实现。

## 完成判定

- IFDS 独立测试覆盖普通边、调用边、返回边、旁路边、回调、多目标、循环和递归。
- 所有事实集合均遵循 `factEqual` 定义的语义相等。
- 空指针与资源泄漏分析迁移后，优化前后的诊断差异均已分类说明。
- 大型真实项目上不存在明显的路径边重复膨胀，且求解统计能够解释截断或超时。
- `src/ifds` 及具体分析器不直接导入 `arkanalyzer` 或 `arkanalyzer/lib/...`，统一经过适配层。
